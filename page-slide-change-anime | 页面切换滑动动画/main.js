const CONTAINER = document.querySelector("#container")
let moved = 0

function prePage() {
    moved
}
function nextPage() {
    moved -= 110
    CONTAINER.getElementsByClassName.left = moved + "vw"
}